# この作品の進め方

仕様と決めごとは `DESIGN.md`、やったことと理由は `LOG.md` に書いてある。
迷ったらそちらが正で、ここには**作業の作法**だけを書く。

## AIへの問い（OpenAI）は、必要なときだけ投げる

村人の判断は外のモデル（`scripts/autoload/ai.gd`、既定 `gpt-4o-mini`）に訊いていて、
**投げるたびに作者の財布から出る**。検証のために村を45秒動かすと50件を超える。

- 直したのが**文字列・配置・色・尺**のような、答えの中身に関わらないものなら、
  `AI.off = true`（または `AI` を「使わない」に）で確かめる
- **問いの形や答えの読み方を変えたときだけ**、実際に投げて1回確かめる
- 繰り返し測るのは、**数字がその判断の根拠になるときだけ**
  （立ち止まりの割合、問いの数、呼び名の質）。同じ数字を二度取らない
- この機械の中（ollama）は検証用に使える。ただし速さは本番の目安にならない

## 確かめかた

- 起動の確認は `godot --headless --quit-after 60 --path .`（`mise` の 4.7.2）
- 見た目はフルサイズ（1920×1080）で撮る。小さい窓だと 1px の線が潰れる
- `project.godot` に autoload を足したら、**エディタのリロードが要る**と作者に伝える
  （CLI は毎回新しいプロセスなので、こちらでは気づけない）

## コミット

- **言われたときだけコミットする。** 続けて2つ積まない
- 行き止まりになった変更は、履歴に残さず戻す
