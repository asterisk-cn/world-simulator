# World Simulator — システム設計

クォータービューの村シミュレーション。
AI村人が自律的に行動するのを、プレイヤーは**神の視点**から観察し干渉する。

---

## 0. 前提

> **判断はAIが行う。**

キャラクターが何をするか、それをどう受け取るか、その結果自分の中の何が変わるか——
これらはすべてAIの担当で、**プログラムには一つも実装されていない**。

現在 `Brain.choose()` は、実行可能な行動からランダムに1つ返すだけ。
パラメータを読まないし、動かしもしない。AIを繋いだときにこの穴がそのまま埋まる。

これは暫定的なルールベース実装を「あとで差し替える」のではない。
**最初からプログラム側に判断を書かない**という分界であり、
だから効用の式も、パラメータの駆動源も、出来事ごとの変化量も、派生特性も存在しない。

なお、**設計の説明はこのドキュメントにだけ置く。**
ゲーム画面には操作に必要なことしか書かない。

---

## 1. 責務の分界

### 分け方

> **「世界で何が起きたか」はプログラム。「それをどう受け取り、どうするか」はAI。**

客観的事実の測定と執行はプログラムが持つ。主観的な解釈・選択・感じ方はAIが持つ。
この一線を跨がせないことで、AIが世界の法則を捻じ曲げられなくなり、
同時にプログラムが人格を代弁しなくなる。

### プログラムが持つもの

| 領域 | 中身 |
|---|---|
| **世界の物理** | グリッド、移動、資源の残量、建築コスト、時間、昼夜 |
| **可視性** | 誰が誰を見えるか、誰が何を目撃したか。情報の非対称の土台 |
| **実現可能性** | いまその行動ができるか（食料があるか、資材が足りるか、対象が視界内か）。AIに渡す「できることの一覧」を作る |
| **実行** | 選ばれた行動の遂行、在庫の増減、建物の生成、経路移動 |
| **記憶の器** | 出来事の記録、既読管理（【要検討】§6） |
| **定義の保持** | どんなパラメータとアクションが存在するか（開始時に固定される） |
| **UI・干渉の適用** | マトリクス表示、インスペクタ、神の操作の反映 |

### AIが持つもの

| 判断 | 現在 | AI版で渡す文脈 |
|---|---|---|
| **何をするか** | 実行可能な候補からランダム | できることの一覧、自分のパラメータ、相手ごとのパラメータ、性格、記憶 |
| **自分がどう変わったか** | 何もしない | 何をして何が起きたか → どのパラメータがどう動いたか |
| **相手をどう思うか** | 何もしない | 誰が何をしたか → その相手についての値がどう動いたか |
| **何を話すか** | 話した事実だけ記録 | 相手との関係と自分の記憶 → 発話内容、隠すか、嘘をつくか |
| **話を信じるか** | 判定なし | 話し手についての値、話の内容、自分の知っていること |
| **掲示板に何を書くか** | 木の実の場所（事実）のみ | 自分の状態、村の状況 → 文面と種類 |
| **掲示板をどう読んだか** | 読んだ事実だけ記録 | 文面、書いた相手についての値 |
| **何を覚え何を忘れるか** | 直近N件を機械的に要約 | その日の出来事すべて → 要約 |
| **どこへ行くか** | 最近傍 / ランダム | 地形と記憶 → 目的地 |

コード内の該当箇所には `【AI差し替え口】` のコメントがある。

### プレイヤー（神）が持つもの

AIとプログラムのどちらでもない第三の層。

- **この世界にどんなパラメータとアクションが存在するか** — AIに渡す語彙そのもの
- 各アクションが世界に何をするか（タイプと対象の選択）
- 個体の値・性格・相手ごとの値への直接干渉

パラメータを1つ足すことは、**AIが自分について語れる言葉を1つ増やすこと**を意味する。

### 呼び出しの頻度（想定）

| タイミング | 頻度 |
|---|---|
| 行動の選択 | 村人ごとに `行動を選び直す間隔`（既定1.1秒）に1回 |
| 行動の完了 | 行動が終わるたび |
| 相互作用 | 会話・目撃・掲示板のたび |
| 夜 | 1日1回、村人ごと |

村人の内部状態はすべて `Villager` が抱えていて、集合知も共有状態もない。
そのまま「一人につき1コンテキスト」に対応する。

---

## 2. パラメータ

### 個人パラメータと関係パラメータ

構造は同じで、**関わる人数だけが違う**。

| 種類 | 持ち方 | 例 |
|---|---|---|
| **個人パラメータ** | 一人につき1つ | 空腹、睡眠、退屈、孤独 |
| **関係パラメータ** | 相手1人につき1つ | 好感、信頼、敬意、負い目 |

関係パラメータは、n人いれば n×n のグリッドになる。
`A→B` と `B→A` は別のオブジェクトで、同期はされない。

どちらであるかは**そのパラメータがどのリストに属するかで決まる**ので、
定義の中に「対象範囲」という選択項目はない。

### 定義の中身

| 項目 | 説明 |
|---|---|
| 名前 | AIに渡される語 |
| 範囲 | 下限と上限。1本のスライダーで両端を決める |

**これで全部。** 駆動源も、速度も、自然減衰も、出来事ごとの変化量も、
夜の風化も、修飾特性もない。値がどう動くかはAIが決めるので、定義側には何も要らない。

パラメータは**カテゴリの中に足す**ので、個々にカテゴリを選ぶ欄はない。
色もカテゴリで決まる（同じカテゴリの中では明るさが少しずつずれる）ので、選ぶ欄はない。

削除は設定パネル右上の「削除」で**削除モード**に入ってから行う。
普段は × が出ないので、編集中に誤って消えない。世界タブには消せるものが無いので出ない。

### 既定

個人：生存（空腹 / 睡眠 / 不安 / 居住）、好奇心（退屈 / 停滞）、共同体（孤独 / 過密 / 不公平）
関係：親しみ（好感 / 信頼）、評価（敬意 / 負い目）

これは初期値であって仕様ではない。**開始前のセットアップ画面**で追加・削除・改名ができる。

---

## 3. アクション

### 型 × 対象

アクションは **タイプ（動詞）× 対象** で表す。

| 項目 | 説明 |
|---|---|
| 名前 | AIに渡される語 |
| タイプ | 動く / 採取 / 制作 / 建設 / 使用 / 交流 |
| 対象 | タイプごとに選べるものが決まる |

**これで全部。** 効用の係数も、下駄も、特性フィルタも、完了時の変化もない。
いつ選ぶかはAIが決めるので、定義側には何も要らない。

同じタイプ × 対象のアクションを名前違いで複数作れる。
定義できるのはパラメータと同じく開始前だけ。

| タイプ | 対象 | 世界で起きること |
|---|---|---|
| **動く** | 適当な場所 | ランダムな地点へ移動 |
| | 誰かのそばへ | その相手のところへ移動 |
| | 誰かから離れて | その相手と逆方向へ移動 |
| | 自分の家へ | 自分の家へ移動 |
| | 掲示板へ | 掲示板へ移動 |
| **採取** | 木の実 / 木 / 石 | 一番近いその資源へ行って1つ取る |
| **制作** | レシピ | 材料を消して、できたものを1つ持つ |
| **建設** | 家 | 木材と石材を消費して家が建つ |
| **使用** | 食料 | 手持ちの食料が1減る |
| | 自分の家 | 眠る |
| **交流** | 誰かと話す | 双方に会話の事実が記録される |
| | 掲示板に貼る | 貼り紙が1枚増える（1日1枚まで） |
| | 掲示板を読む | 未読が既読になる |

### 距離

対象ごとに**届く範囲**が決まっている。

- **使用**と**交流**は、いま届く範囲になければそもそも候補に出ない。
  遠ければ先に「動く」で近づく必要がある。
- **動く / 採取 / 建設** は目的地まで移動してから行う動作なので、範囲の制限はない。

これによって「掲示板を読むにはまず掲示板へ行く」「話すにはまず近づく」が成立する。

### レシピ

**制作の対象はレシピそのもの。** レシピを1つ足せば、作れるものが1つ増える。

| 項目 | 説明 |
|---|---|
| 名前 | できるものの名前。そのまま持ち物として増える |
| 材料 | 持ち物を何個ずつ使うか |

元からある持ち物は 木の実 / 木材 / 石材 の3つで、採取で手に入る。
レシピで作ったものも材料に使えるので、段を重ねられる。
材料が全部そろっているときだけ「制作」の候補に出る。持ち物で完結するので移動はしない。

### 実現可能性

プログラムは毎回「いまこの村人に何ができるか」を列挙する。
相手を取る対象（誰かのそばへ / 誰かから離れて / 誰かと話す）は、相手ごとに1候補ずつ立つ。

AIにはこの一覧が渡り、どれを選ぶかを答える。

---

## 4. 性格

MBTI 4軸を連続値で保持（離散ラベルではないのでスライダーで直接いじれる）＋ 一言個性。

**派生特性はない。** ここから行動への影響は一切導出されない。
これはAIに「この人はこういう人だ」と渡すための素の記述であって、
プログラムが読んで何かを決めるためのものではない。

---

## 5. 限定されたコミュニケーション

**集合知は存在しない。** 他人のことは以下からしか知り得ない。

- **会話** — 近くの誰かに話しかける。いまは接触した事実だけが双方に記録される
- **掲示板** — 貼り紙を貼る / 読む。いまは木の実の場所（観測した事実）だけが貼られる
- **目撃** — 視界内で起きたことが記録される

何を話すか、何を書くか、それをどう受け取るかはAIの担当なので、現在は穴になっている。

### 神の貼り紙

プレイヤーの貼り紙は **差出人不明** として扱われる。村への唯一の間接的な干渉手段。

---

## 6. 記憶 【要検討】

**扱いが未決の領域。** 現在は暫定で以下のようになっている。

- 出来事をそのまま溜める
- 夜になったら直近N件を1行の要約に畳み、保持日数を超えた要約は捨てる

何を覚えていて何を忘れるかはAIの判断のはずで、
その場合この機械的な圧縮は不要になる。一方でAIに渡すコンテキスト量を抑える実務的な要請もある。
**どちらに寄せるかは未決定。**

---

## 7. プレイヤー（神）にできること

村の中に自分の駒はない。干渉は**開始前**と**進行中**で分かれる。

### 開始前（セットアップ）

世界は止まっていて、村人もまだいない。ここで決めるのは**この世界の語彙**。

| できること | 場所 |
|---|---|
| カテゴリとパラメータの追加 / 削除 / 改名、範囲の変更 | 設定「パラメータ」 |
| レシピの追加 / 削除 / 改名、材料の変更 | 設定「レシピ」 |
| アクションの追加 / 削除 / 改名 / タイプと対象の変更 | 設定「アクション」 |
| 世界のルール（1日の長さ、夜の割合、行動を選び直す間隔、移動速度、記憶の保持量） | 設定「世界」 |

「この世界を始める」を押すと村人が置かれ、時間が動き出す。

### 進行中

**定義もパラメータの値も変更できない。** 見るだけ。

| できること | 場所 |
|---|---|
| 個体の値・性格の閲覧 | 村人をクリック |
| n×n グリッドの閲覧 | 上部「関係マトリクス」 |
| 差出人不明の貼り紙 | 上部「掲示板」 |
| 設定の閲覧（パラメータ / アクション / レシピ / 世界） | 上部「設定」 |
| デバッグ（在庫一覧・材料配布・家を建てる） | 上部「デバッグ」 |

### なぜ開始前に閉じるのか

パラメータとアクションはAIに渡す語彙そのものなので、途中で語彙が変わると
それ以前の村人の記憶や値と辻褄が合わなくなる。
**語彙は世界の前提として先に固定する。**

> **【未決】** 値も開始前だけにした結果、いま神が村人に対してできる干渉は
> 差出人不明の貼り紙だけになっている。村人はまだセットアップ画面に存在しないので、
> 初期値や性格を仕込む場所もない。「村人セットアップ」を開始前に足すか、
> 進行中の値干渉をどこかで許すかは未決定。

---

## 8. ファイル構成

```
scripts/
  autoload/
	schema.gd        パラメータとアクションの定義。プレイヤーが書き換える対象
	sim_config.gd    世界のルール値。設定「世界」で開始前に決める
	sim_clock.gd     昼夜と日付
	event_log.gd     村の記録（`-- --echo-log` で標準出力にも流れる）
  data/
	self_params.gd   自分スコープの値
	pair_params.gd   相手スコープの値（1相手ぶん）
	personality.gd   MBTI 4軸と一言個性
	memory.gd        出来事・要約・既読（【要検討】）
	rules.gd         建築コストや視界など固定の物理
  world/
	world.gd         グリッド、資源、建物、掲示板、描画
	iso.gd           クォータービュー変換
	harvest_node.gd / structure.gd / bulletin_board.gd
  villager/
	villager.gd      村人本体。世界で何が起きたかだけを持つ
	brain.gd         判断の置き場所。いまは実現可能性の列挙とランダム選択のみ
  ui/
	ui_kit.gd        共通ウィジェット
	inspector.gd     個体インスペクタ
	matrix_panel.gd  n×n マトリクス
	rules_panel.gd   設定（パラメータ / アクション / レシピ / 世界）。開始前だけ編集可
	range_slider.gd  下限と上限をつまみ2つで決めるスライダー
	debug_panel.gd   村人の在庫・行動の一覧と、材料配布などの近道
	hud.gd           上部バー・記録・掲示板
  main.gd            セットアップ → 開始 → 進行の流れ
```

---

## 9. 世界の物理

- 家は 2x2 の敷地を占め、**他人は通り抜けられない**。家どうしは必ず間隔が空くので、間を通る
- 自分の家の中には入れる（眠るため）
- 移動は AStarGrid2D による経路探索。目的地が塞がっていれば手前で止まる
- 資源（木の実 / 木 / 石）は時間で回復する

### 現在この世界にないもの

暴力・殺害・死亡・HP、他人の家への侵入、遺恨、噂、ストレス、チェスト、持ち物の受け渡し。
村人は死なない。

---

## 10. 実行

```bash
mise exec -- godot --path .
```

Godot 4.7.2 を `mise.toml` で固定している。ヘッドレスで観察するには：

```bash
mise exec -- godot --headless --fixed-fps 60 --quit-after 40000 -- --echo-log --autostart
```

`--autostart` はセットアップ画面を飛ばして既定の定義のまま始める。

| 操作 | 動作 |
|---|---|
| 左クリック | 村人を選択 |
| 右 / 中ドラッグ | カメラ移動 |
| ホイール | ズーム |
| Space | 一時停止 |
