class_name Rules
extends RefCounted
## 固定ルール定数。Villager と Brain が相互参照して循環しないよう、ここに分離している。
##
## 建築コストはここに無い。何をどれだけ使うかは、作る人が持ち物を見て決める。

## 見える範囲。**候補に立つのはここまで**（人も、そこに在る物も）。
## 9 だと資源に届かず、採る手が 16%→3% に落ちた（実測）。13 で届くようになる。
## 覚えている場所はこの外でも候補に立つ（`Memory.seen`）——物は動かないので
const SIGHT := 13.0
